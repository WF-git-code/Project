# 预约系统 - 文档中心

欢迎来到预约系统文档中心！

## 文档列表



| 文档名称 | 文件名 | 说明 |
|---------|--------|------|
| 项目总览 | [README.md](README.md) | 项目整体介绍、日志清理功能说明 |
| 系统配置指南 | [系统配置指南.md](系统配置指南.md) | 完整的配置项说明、场景配置方案 |
| 线程池配置指南 | [线程池配置指南.md](线程池配置指南.md) | 线程池参数详解、性能优化建议 |
| 日志清理使用说明 | [日志清理使用说明.md](日志清理使用说明.md) | 日志清理功能详细说明 |

##  快速开始

### 新手推荐阅读顺序

1. [系统配置指南](系统配置指南.md) - 掌握配置方法docs目录下
2. [线程池配置指南](线程池配置指南.md) - 优化性能docs目录下
3. [日志清理使用说明](日志清理使用说明.md) - 管理日志docs目录下

## 配置示例速查

### 开发环境

```ini
ip 127.0.0.1
port 6000
lismax 512
corethreadnum 2
maxthreadnum 8
taskmax 512
taskwaittime 10
threadwaittime 10
enablelogclean 1
logkeepdays 7
logdir ./
```

---

运行

c/c++ 环境    linux   Ubuntu20.4包

注意redis配置和MySQL配置

数据库		版本		说明
Redis		5.0.7		hiredis 5.2.1
MySQL		8.0.42		Ubuntu 20.04 包

```bash
cd build 
make clean
make
cd ../bin
./server
./client
```

### 1.1 项目简介

这是一个基于C++14的**高性能预约票务系统**，采用经典的服务器架构：

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              预约系统架构图                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                             │
│  │ Client 1 │  │ Client 2 │  │ Client N │  (多个客户端)               │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘                             │
│       │             │             │                                   │
│       └─────────────┴─────────────┘                                   │
│                     │ TCP连接                                        │
│         ┌───────────▼───────────┐                                    │
│         │   Epoll事件循环       │                                    │
│         │   (主事件循环线程)     │                                    │
│         └───────────┬───────────┘                                    │
│                     │ 提交任务                                       │
│         ┌───────────▼───────────┐                                    │
│         │   CachedThreadPool    │  (缓存式线程池)                     │
│         │   (Worker线程池)       │                                    │
│         └───────────┬───────────┘                                    │
│                     │                                                │
│         ┌───────────┼───────────────────────────────────────┐        │
│         │           │                                       │        │
│         │  ┌────────▼────────┐    ┌────────────────────┐   │        │
│         │  │ Redis连接池     │    │ MySQL连接池        │   │        │
│         │  │ (缓存+分布式锁) │    │ (数据持久化)       │   │        │
│         │  └────────┬────────┘    └────────┬───────────┘   │        │
│         │           │                      │               │        │
│         │  ┌────────▼────────┐    ┌────────▼───────────┐   │        │
│         │  │   Redis         │    │   MySQL数据库      │   │        │
│         │  │ (缓存/锁/会话)   │    │ (票务/用户/预约)   │   │        │
│         │  └─────────────────┘    └────────────────────┘   │        │
│         └──────────────────────────────────────────────────┘        │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │  辅助系统                                                       │  │
│  │  ┌─────────────────────────────────────────────────────────┐  │  │
│  │  │  异步日志系统 (AsynLogging) - 双缓冲，后台线程写磁盘  │  │  │
│  │  └─────────────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────────────┐  │  │
│  │  │  日志清理器 (LogCleaner) - 定时删除过期日志            │  │  │
│  │  └─────────────────────────────────────────────────────────┘  │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 核心技术亮点

| 技术点               | 说明                                 | 为什么重要                  |
| -------------------- | ------------------------------------ | --------------------------- |
| Epoll + EPOLLONESHOT | 高效I/O多路复用，保证线程安全        | 高并发场景必备              |
| 缓存式线程池         | 核心线程常驻，非核心线程动态扩容回收 | 高峰低峰自适应              |
| MySQL连接池          | 复用连接，减少握手开销               | 数据库操作性能提升10倍+     |
| Redis连接池          | 复用Redis连接，支持缓存和分布式锁    | 缓存命中率提升，锁竞争降低  |
| 票务信息缓存         | Redis缓存ticket:list，TTL=300秒      | 减少数据库查询，QPS提升5倍+ |
| 分布式锁             | SET NX PX实现，防止超卖              | 高并发预约场景核心保障      |
| 缓存一致性           | 先更新数据库再删缓存                 | 保证数据准确性              |
| 双缓冲异步日志       | 前台线程写内存，后台线程写磁盘       | 不阻塞业务，延迟低          |
| 事务 + 乐观锁        | 保证数据一致性，防止超卖             | 预约系统核心                |
| RAII智能管理         | Socket/Connection自动释放            | 避免资源泄露                |

### 目录结构详解

```
linux_new_project/
├── CMakeLists.txt                          # CMake构建配置
├── my.conf                                 # 全局配置文件
├── init_database.sql                       # 数据库初始化脚本
├── server/                                 # 服务器代码（核心重点）
│   ├── server.h                           # 服务器头文件（类定义）
│   ├── server.cpp                         # 服务器实现（主逻辑）
│   ├── ThreadPool/                        # 线程池模块
│   │   ├── CachedThreadPool.hpp           # 缓存式线程池（核心）
│   │   └── TaskQueue.hpp                  # 任务队列
│   ├── LogFile/                           # 日志系统
│   │   ├── include/                       # 日志头文件
│   │   └── src/                           # 日志实现
│   ├── MysqlConnectionPool.hpp            # MySQL连接池（核心）
│   └── RedisConnectionPool.hpp            # Redis连接池（核心）
├── client/                                 # 客户端代码
│   ├── client.h
│   └── client.cpp
├── demo/                                   # 演示程序
│   └── LogCleanerDemo.cpp
├── docs/                                   # 文档（现在看的这个）
│   └── 超详细项目学习与面试指南.md
└── bin/                                    # 编译输出（cmake后生成）
    ├── server
    ├── client
    └── log_demo
```

### 启动流程详解

```
┌─────────────────────────────────────────────────────────────────────┐
│ Step 1: 解析命令行参数                                               │
│         支持指定配置文件路径（默认 ../my.conf）                     │
└────────────────────────────────────┬────────────────────────────────┘
                                     │
┌────────────────────────────────────▼────────────────────────────────┐
│ Step 2: 加载配置文件                                                 │
│         读取 my.conf，初始化 ServerConfig 类                        │
│         包括：IP/端口、线程池参数、数据库配置、日志配置、Redis配置   │
└────────────────────────────────────┬────────────────────────────────┘
                                     │
┌────────────────────────────────────▼────────────────────────────────┐
│ Step 3: 初始化日志系统                                               │
│         LoggerManager::init(...)                                    │
│         LoggerManager::start()      (启动后台线程)                  │
│         LoggerManager::setOutput()  (设置输出回调)                  │
└────────────────────────────────────┬────────────────────────────────┘
                                     │
┌────────────────────────────────────▼────────────────────────────────┐
│ Step 4: 初始化MySQL连接池                                            │
│         MysqlConnectionPool::getInstance().init(...)                │
│         预创建 db_pool_size(默认8) 个连接                          │
└────────────────────────────────────┬────────────────────────────────┘
                                     │
┌────────────────────────────────────▼────────────────────────────────┐
│ Step 5: 初始化Redis连接池                                            │
│         RedisConnectionPool::getInstance().init(...)                │
│         预创建 redis_pool_size(默认4) 个连接                        │
│         支持缓存和分布式锁功能                                      │
└────────────────────────────────────┬────────────────────────────────┘
                                     │
┌────────────────────────────────────▼────────────────────────────────┐
│ Step 6: 创建TCP服务器对象                                            │
│         TcpServer ser(conf)                                          │
└────────────────────────────────────┬────────────────────────────────┘
                                     │
┌────────────────────────────────────▼────────────────────────────────┐
│ Step 7: 初始化服务器                                                 │
│         ser.Ser_Init()                                              │
│         ├─ 创建Socket: socket()                                     │
│         ├─ 设置端口复用: setsockopt(SO_REUSEADDR)                   │
│         ├─ 绑定地址: bind()                                         │
│         ├─ 开始监听: listen()                                       │
│         ├─ 创建Epoll: epoll_create()                                │
│         └─ 注册监听Socket: epoll_ctl(EPOLL_CTL_ADD)                 │
└────────────────────────────────────┬────────────────────────────────┘
                                     │
┌────────────────────────────────────▼────────────────────────────────┐
│ Step 8: 进入主事件循环（永不退出）                                   │
│         ser.Run()                                                    │
│         while(true) {                                               │
│             epoll_wait(...)        // 等待事件                      │
│             do_events()             // 处理事件                      │
│         }                                                          │
└─────────────────────────────────────────────────────────────────────┘
```

